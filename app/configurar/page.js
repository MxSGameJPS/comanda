import FirstAdminSetup from "@/Components/FirstAdminSetup/first-admin-setup";

export const metadata = { title: "Configuração inicial" };

export default function SetupPage() {
  return <FirstAdminSetup />;
}
